# Timp real — plan de implementare

> **Pentru cine execută:** SUB-SKILL OBLIGATORIU — folosește
> `superpowers:subagent-driven-development` (recomandat) sau
> `superpowers:executing-plans` ca să execuți planul sarcină cu sarcină.
> Pașii folosesc casete (`- [ ]`) pentru urmărire.

**Scop:** Tot ce vine din exterior (resurse de la admin, redenumire, anunțuri,
blocări, cereri de prietenie, mesaje, ban) ajunge la jucător fără repornire,
iar orice ecran cu balanță se reîmprospătează singur.

**Arhitectură:** Fiecare serviciu primește `startLive()`/`stopLive()` pentru
domeniul lui; un singur coordonator legat de `FirebaseAuth.userChanges()` le
pornește și oprește. Balanța capătă un punct unic de scriere
(`_writeBalance`) plus un contor pe care ascultă ecranele. Numele trece de la
„lanț" la „valoare de pornire", cu o funcție de prioritate pură și testabilă.

**Stack:** Flutter/Dart, `cloud_firestore`, `firebase_auth`,
`shared_preferences`. Teste: `flutter test`. Verificare pe viu: Playwright
(două contexte Chrome) + panoul de Admin al aplicației.

**Spec:** `docs/superpowers/specs/2026-08-31-timp-real-design.md` — se
citește împreună cu planul; planul argumentează din el.

## Constrângeri globale

- **Linia de bază, măsurată înainte de prima modificare:** `flutter analyze`
  → „No issues found!"; `flutter test` → 199 teste verzi. Orice abatere de la
  ea, în orice sarcină, e o regresie și se oprește acolo.
- **Limba codului:** comentariile și textele din interfață se scriu în
  română, ca restul proiectului. Textele vizibile trec prin `tr('ro', 'en')`.
- **Nicio scriere directă pe cheile de balanță.** După Sarcina 1,
  `prefs.setInt` pe `_coinsKey`, `_gemsKey`, `_livesKey`, `_hintsKey`,
  `_xpKey` e interzis; se folosește `_writeBalance`.
- **Gardă Firebase obligatorie** în orice cod care atinge
  `FirebaseAuth.instance` de la rădăcina aplicației:
  `if (Firebase.apps.isEmpty) return;` — altfel `test/widget_test.dart` pică
  (precedent documentat la `main.dart:303-307`).
- **Niciun deploy de reguli Firestore fără OK explicit** de la proprietar.
- **Fără `-r` la instalarea pe telefon** — instalare clean, progresul e
  consumabil până la lansare.
- Commit după fiecare sarcină. Fără push fără OK explicit.

---

## Sarcina 1: Punctul unic de scriere a balanței

**Fișiere:**
- Modifică: `lib/data/storage_service.dart`
- Test: `test/balance_revision_test.dart` (creează)

**Interfețe:**
- Produce: `StorageService.balanceRevision` (`ValueNotifier<int>`), citit de
  toate ecranele în Sarcina 2.

- [ ] **Pasul 1: Scrie testul care pică**

Creează `test/balance_revision_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:guess_it/data/storage_service.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUp(() async {
    SharedPreferences.setMockInitialValues({});
    StorageService.balanceRevision.value = 0;
  });

  test('fiecare scriere de balanta creste contorul', () async {
    final start = StorageService.balanceRevision.value;
    await StorageService.addCoins(10);
    expect(StorageService.balanceRevision.value, greaterThan(start),
        reason: 'addCoins trebuie sa anunte ecranele');

    final afterCoins = StorageService.balanceRevision.value;
    await StorageService.addGems(1);
    expect(StorageService.balanceRevision.value, greaterThan(afterCoins));

    final afterGems = StorageService.balanceRevision.value;
    await StorageService.spendHint();
    expect(StorageService.balanceRevision.value, greaterThan(afterGems),
        reason: 'un hint consumat schimba ce vede jucatorul');
  });

  test('golirea si reimportul cresc contorul, desi nu ating chei literale', () async {
    await StorageService.addCoins(500);

    var before = StorageService.balanceRevision.value;
    await StorageService.resetToStartingBalance();
    expect(StorageService.balanceRevision.value, greaterThan(before),
        reason: 'resetul de admin schimba balanta prin prefs.clear(), fara setInt');

    before = StorageService.balanceRevision.value;
    await StorageService.resetAll();
    expect(StorageService.balanceRevision.value, greaterThan(before),
        reason: 'stergerea totala din Profil schimba tot');

    before = StorageService.balanceRevision.value;
    await StorageService.importAll({'coins': 999});
    expect(StorageService.balanceRevision.value, greaterThan(before),
        reason: 'cloud-ul care coboara la logare schimba balanta');
  });
}
```

- [ ] **Pasul 2: Rulează testul, confirmă că pică**

Rulează: `flutter test test/balance_revision_test.dart`
Așteptat: EȘEC — `balanceRevision` nu există (eroare de compilare).

- [ ] **Pasul 3: Adaugă contorul și punctul unic de trecere**

În `lib/data/storage_service.dart`, în clasa `StorageService`, lângă
constantele de chei:

```dart
  /// Crește la FIECARE schimbare de balanță (monede, gems, vieți, hint-uri,
  /// XP), indiferent de cauză: cumpărătură, premiu, roată, grant de la admin,
  /// reset, sau salvarea coborâtă din cloud. Ecranele care afișează resurse
  /// ascultă direct valoarea asta.
  ///
  /// A înlocuit `CloudSyncService.grantsApplied`, care anunța doar
  /// grant-urile de la admin și era ascultat doar de HomeScreen — un jucător
  /// aflat în Magazin rămânea cu cifrele vechi și primea „n-ai destui bani"
  /// pentru bani pe care îi avea deja.
  static final ValueNotifier<int> balanceRevision = ValueNotifier<int>(0);

  /// SINGURA cale prin care se scrie o cheie de balanță. Regula, pe termen
  /// lung: `prefs.setInt` pe `_coinsKey`/`_gemsKey`/`_livesKey`/`_hintsKey`/
  /// `_xpKey` nu se mai folosește nicăieri. Așa nu poate exista o funcție
  /// nouă care schimbă balanța și uită să anunțe ecranele.
  static Future<void> _writeBalance(SharedPreferences prefs, String key, int value) async {
    await prefs.setInt(key, value);
    balanceRevision.value++;
  }
```

Adaugă importul necesar în capul fișierului:

```dart
import 'package:flutter/foundation.dart';
```

- [ ] **Pasul 4: Treci cele 16 scrieri prin `_writeBalance`**

Înlocuiește, în `lib/data/storage_service.dart`, fiecare apel de mai jos.
Forma e mereu aceeași: `await prefs.setInt(CHEIE, VALOARE);` devine
`await _writeBalance(prefs, CHEIE, VALOARE);`.

| Linie | Funcția |
|---|---|
| `:160` | `setLives` |
| `:191` | `_rechargeLives` |
| `:265` | `addCoins` |
| `:273` | `spendCoins` |
| `:289` | `adjustCoins` |
| `:305` | `addGems` |
| `:313` | `spendGems` |
| `:323` | `adjustGems` |
| `:375` | `addHints` |
| `:385` | `addHintsUncapped` |
| `:394` | `adjustHints` |
| `:402` | `spendHint` |
| `:419` | `addXp` |
| `:430` | `adjustXp` |
| `:449` | `_migrateXpCurveIfNeeded` |
| `:469` | `_migrateXpRateV3` |

Două note care NU se „repară", ci se lasă intenționat:

- `_rechargeLives` e chemat și din citiri (`getLives` la `:132`,
  `livesRechargeRemaining` la `:142`), deci o citire poate crește contorul.
  Converge: scrie doar când chiar au trecut minutele de regenerare, cel mult
  o dată la ~23 de minute.
- Migrările de XP (`:449`, `:469`) sunt lazy, nu „la pornire", și se
  rearmează după orice `prefs.clear()`. Trec prin `_writeBalance` ca oricare
  altă scriere.

- [ ] **Pasul 5: Adaugă bump explicit unde nu există chei literale**

Cele trei funcții care schimbă balanța fără să atingă vreo cheie literală
NU pot fi prinse de pasul anterior. În `lib/data/storage_service.dart`:

```dart
  /// Șterge tot progresul salvat (folosit din ecranul de profil).
  static Future<void> resetAll() async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.clear();
    // `prefs.clear()` nu trece prin [_writeBalance] — nu scrie nicio cheie,
    // le șterge. Balanța se schimbă totuși (getterele cad pe valorile de
    // start), deci ecranele trebuie anunțate explicit.
    balanceRevision.value++;
  }
```

```dart
  static Future<void> resetToStartingBalance() async {
    final prefs = await SharedPreferences.getInstance();
    final preserved = <String, dynamic>{};
    for (final key in _resetPreservedKeys) {
      final value = prefs.get(key);
      if (value != null) preserved[key] = value;
    }
    await prefs.clear();
    await importAll(preserved);
    // Vezi nota din [resetAll]: ștergerea de chei nu produce nicio scriere
    // de balanță, dar schimbă tot ce se vede pe ecran.
    balanceRevision.value++;
  }
```

În `importAll` (`:1794`), la finalul funcției, după bucla de scriere:

```dart
    // Cheia scrisă aici e variabilă la runtime, deci nu se poate ști static
    // dacă a fost una de balanță. Salvarea coborâtă din cloud la logare
    // schimbă aproape sigur balanța, deci se anunță necondiționat.
    balanceRevision.value++;
```

- [ ] **Pasul 6: Rulează testul, confirmă că trece**

Rulează: `flutter test test/balance_revision_test.dart`
Așteptat: ambele teste TREC.

- [ ] **Pasul 7: Confirmă că nu s-a rupt nimic**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 201 teste verzi (199 + 2 noi).

- [ ] **Pasul 8: Commit**

```bash
git add lib/data/storage_service.dart test/balance_revision_test.dart
git commit -m "Balanta anunta o singura data, dintr-un singur loc"
```

---

## Sarcina 2: Ecranele ascultă contorul de balanță

**Fișiere:**
- Modifică: `lib/screens/home_screen.dart`, `lib/screens/profile_screen.dart`,
  `lib/screens/shop_screen.dart`, `lib/screens/categories_screen.dart`,
  `lib/screens/game_screen.dart`, `lib/screens/quests_screen.dart`,
  `lib/screens/achievements_screen.dart`,
  `lib/screens/planet_hologram_screen.dart`,
  `lib/widgets/lives_countdown_card.dart`,
  `lib/widgets/match_stake_dialog.dart`
- Modifică: `lib/data/cloud_sync_service.dart` (scoate `grantsApplied`)

**Interfețe:**
- Consumă: `StorageService.balanceRevision` din Sarcina 1.

- [ ] **Pasul 1: Mută `HomeScreen` de pe `grantsApplied` pe contorul nou**

În `lib/screens/home_screen.dart`, înlocuiește cele două linii existente
(`:60` și `:71`):

```dart
    // înainte: CloudSyncService.instance.grantsApplied.addListener(_refresh);
    StorageService.balanceRevision.addListener(_refresh);
```

```dart
    // înainte: CloudSyncService.instance.grantsApplied.removeListener(_refresh);
    StorageService.balanceRevision.removeListener(_refresh);
```

- [ ] **Pasul 2: Adaugă același ascultător în celelalte ecrane cu balanță**

Pentru fiecare din fișierele rămase, în `initState` și `dispose` ale stării
care încarcă datele. Tiparul, identic peste tot (exemplu pentru
`shop_screen.dart`, unde metoda de reîncărcare se numește `_refresh` — dacă
în alt ecran se numește altfel, folosește numele de acolo):

```dart
  @override
  void initState() {
    super.initState();
    // Resursele se pot schimba sub ecranul deschis: un grant de la admin, un
    // premiu încasat în fundal, sau salvarea coborâtă din cloud. Vezi
    // StorageService.balanceRevision.
    StorageService.balanceRevision.addListener(_refresh);
  }

  @override
  void dispose() {
    StorageService.balanceRevision.removeListener(_refresh);
    super.dispose();
  }
```

Dacă ecranul nu are încă o metodă de reîncărcare, folosește tiparul lui de
`FutureBuilder`:

```dart
  void _refresh() {
    if (!mounted) return;
    setState(() => _dataFuture = _load());
  }
```

Adaugă importul `import '../data/storage_service.dart';` unde lipsește
(pentru widget-uri: `import '../data/storage_service.dart';`).

- [ ] **Pasul 3: Șterge `grantsApplied`, acum fără ascultători**

În `lib/data/cloud_sync_service.dart`, șterge câmpul `grantsApplied` (`:33`,
împreună cu blocul lui de documentație `:28-32`) și linia
`grantsApplied.value++;` (`:237`).

La `:236-237`, comentariul „Ecranele deschise nu știu că balanța de sub ele
s-a schimbat" se șterge odată cu linia: balanța se anunță acum singură, din
`StorageService`, fiindcă `consumePendingGrant` cheamă `adjustCoins`,
`adjustGems`, `setLives`, `adjustHints`, `adjustXp` și
`resetToStartingBalance` — toate trec prin `_writeBalance` sau au bump
explicit.

- [ ] **Pasul 4: Confirmă**

Rulează: `flutter analyze` → „No issues found!" (prinde orice referință
rămasă la `grantsApplied`).
Rulează: `flutter test` → 201 verzi.

- [ ] **Pasul 5: Commit**

```bash
git add lib/screens lib/widgets lib/data/cloud_sync_service.dart
git commit -m "Toate ecranele cu resurse se reimprospateaza singure"
```

---

## Sarcina 3: Prioritatea numelui, ca funcție pură

**Fișiere:**
- Creează: `lib/core/display_name.dart`
- Modifică: `lib/data/storage_service.dart` (getter fără efecte secundare +
  `_resetPreservedKeys`)
- Modifică: `lib/data/auth_service.dart` (`multiplayerIdentity`)
- Test: `test/display_name_test.dart` (creează),
  `test/account_reset_test.dart` (extinde)

**Interfețe:**
- Produce: `resolveDisplayName({forcedName, chosenName, googleName})` → `String`
- Produce: `StorageService.getChosenDisplayName()` → `Future<String>` (`''`
  dacă jucătorul nu și-a ales niciodată un nume)
- Produce: `StorageService.setChosenDisplayName(String name)`
- Consumat de: Sarcina 4 (ecranele), Sarcina 6 (abonamentul pe profil)

- [ ] **Pasul 1: Scrie testul care pică**

Creează `test/display_name_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:guess_it/core/display_name.dart';

void main() {
  group('resolveDisplayName', () {
    test('numele pus de admin bate tot', () {
      expect(
        resolveDisplayName(forcedName: 'Andrei', chosenName: 'AlesDeMine', googleName: 'Nume Google'),
        'Andrei',
      );
    });

    test('numele ales de jucator bate contul Google', () {
      expect(
        resolveDisplayName(forcedName: '', chosenName: 'AlesDeMine', googleName: 'Nume Google'),
        'AlesDeMine',
      );
    });

    test('fara nume ales, ramane cel din contul Google', () {
      expect(
        resolveDisplayName(forcedName: '', chosenName: '', googleName: 'Nume Google'),
        'Nume Google',
      );
    });

    test('Guest fara nimic: intoarce gol, apelantul cade pe numele local', () {
      expect(resolveDisplayName(forcedName: '', chosenName: '', googleName: ''), '');
    });

    test('spatiile nu tin loc de nume ales', () {
      expect(
        resolveDisplayName(forcedName: '', chosenName: '   ', googleName: 'Nume Google'),
        'Nume Google',
      );
    });
  });
}
```

- [ ] **Pasul 2: Rulează testul, confirmă că pică**

Rulează: `flutter test test/display_name_test.dart`
Așteptat: EȘEC — `lib/core/display_name.dart` nu există.

- [ ] **Pasul 3: Scrie funcția pură**

Creează `lib/core/display_name.dart`:

```dart
/// Care nume se vede, când există mai multe surse.
///
/// Ordinea, și de ce:
///
///  1. `forcedName` — numele pus de administrator din panoul de Admin. Nu e o
///     pedeapsă și nu blochează nimic: jucătorul își poate pune oricând
///     altceva, moment în care numele impus se șterge (vezi
///     PlayerProfileService.releaseMyForcedName). Există doar ca redenumirea
///     să ȚINĂ până atunci — fără el, primul heartbeat al jucătorului i-ar
///     rescrie numele din identitatea locală și redenumirea s-ar evapora
///     singură în câteva minute.
///  2. numele ales chiar de jucător;
///  3. numele din contul Google, ca simplă valoare de pornire.
///
/// Șir gol înseamnă „nicio sursă" — apelantul cade atunci pe numele local
/// generat (vezi StorageService.getDisplayName).
///
/// E deliberat o funcție PURĂ, fără citiri de disc: sursele se citesc de
/// apelant. Așa se poate testa fiecare combinație fără SharedPreferences,
/// iar regula stă într-un singur loc în tot proiectul.
String resolveDisplayName({
  required String forcedName,
  required String chosenName,
  required String googleName,
}) {
  if (forcedName.trim().isNotEmpty) return forcedName.trim();
  if (chosenName.trim().isNotEmpty) return chosenName.trim();
  return googleName.trim();
}
```

- [ ] **Pasul 4: Rulează testul, confirmă că trece**

Rulează: `flutter test test/display_name_test.dart`
Așteptat: 5 teste TREC.

- [ ] **Pasul 5: Adaugă cititorul fără efecte secundare**

În `lib/data/storage_service.dart`, lângă cheile existente:

```dart
  static const _displayNameChosenKey = 'display_name_chosen';
```

Și metodele, lângă `getDisplayName`/`setDisplayName`:

```dart
  /// Numele pe care jucătorul chiar și l-a ales, sau `''` dacă n-a ales
  /// niciodată unul.
  ///
  /// DE CE NU SE FOLOSEȘTE [getDisplayName] pentru asta: acela GENEREAZĂ și
  /// SALVEAZĂ un nume („JucatorNNN") când cheia lipsește. Sub noua ordine a
  /// numelor, întrebarea „și-a ales un nume?" se pune la fiecare rezolvare de
  /// identitate, inclusiv pentru conturi Google — deci [getDisplayName] ar
  /// scrie acel nume generat în contul fiecărui jucător Google care n-avea
  /// unul. Ar intra apoi în cloud-save (exportAll), ar strica amprenta din
  /// CloudSyncService.push și ar produce o scriere Firestore în plus la
  /// fiecare pornire — pe lângă că ar crea exact numele-gunoi pe care noua
  /// ordine încearcă să-l evite.
  static Future<String> getChosenDisplayName() async {
    final prefs = await SharedPreferences.getInstance();
    if (!(prefs.getBool(_displayNameChosenKey) ?? false)) return '';
    return prefs.getString(_displayNameKey) ?? '';
  }

  /// Jucătorul și-a ales singur numele — de acum al lui bate numele din
  /// contul Google (vezi core/display_name.dart).
  static Future<void> setChosenDisplayName(String name) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(_displayNameKey, name);
    await prefs.setBool(_displayNameChosenKey, true);
  }
```

- [ ] **Pasul 6: Salvează marcajul de la un reset de admin**

În `_resetPreservedKeys` (`:605-618`), adaugă cheia:

```dart
    _displayNameKey,
    // Fără ea, un reset de admin ar păstra numele ales (e chiar deasupra) dar
    // ar pierde marcajul că a fost ales — iar jucătorul ar cădea înapoi pe
    // numele din contul Google, adică ar fi REDENUMIT în tăcere de o simplă
    // apăsare pe „Reset". Cele două chei trebuie să călătorească împreună.
    _displayNameChosenKey,
```

- [ ] **Pasul 7: Testează supraviețuirea la reset**

În `test/account_reset_test.dart`, adaugă:

```dart
  test('resetul de admin nu redenumeste jucatorul', () async {
    SharedPreferences.setMockInitialValues({});
    await StorageService.setChosenDisplayName('NumeleMeu');
    expect(await StorageService.getChosenDisplayName(), 'NumeleMeu');

    await StorageService.resetToStartingBalance();

    expect(await StorageService.getChosenDisplayName(), 'NumeleMeu',
        reason: 'un reset de balanta nu are voie sa schimbe numele ales');
  });
```

Rulează: `flutter test test/account_reset_test.dart`
Așteptat: TREC toate, inclusiv cel nou.

- [ ] **Pasul 8: Leagă funcția pură de identitate**

În `lib/data/auth_service.dart`, înlocuiește corpul lui
`multiplayerIdentity()` (`:82-99`):

```dart
  Future<({String name, String? photoUrl, String avatarStyle})> multiplayerIdentity() async {
    final avatarStyle = await StorageService.getAvatarStyleId();
    final u = currentUser;
    // Ordinea numelor stă într-un singur loc, testabil: core/display_name.dart.
    final resolved = resolveDisplayName(
      forcedName: await StorageService.getForcedName(),
      chosenName: await StorageService.getChosenDisplayName(),
      googleName: u?.displayName ?? '',
    );
    // Gol înseamnă „nicio sursă": Guest care nu și-a ales încă nimic. Abia
    // aici se cade pe numele local generat — [getDisplayName] îl și creează
    // dacă lipsește, deci nu are voie să fie chemat pe calea de mai sus.
    final name = resolved.isNotEmpty ? resolved : await StorageService.getDisplayName();
    return (name: name, photoUrl: u?.photoURL, avatarStyle: avatarStyle);
  }
```

Adaugă importul: `import '../core/display_name.dart';`

Șterge comentariul vechi de la `:85-88` („Numele pus de administrator bate
tot… E o decizie de moderare"): nu mai e o decizie de moderare, iar
explicația trăiește acum în `core/display_name.dart`.

- [ ] **Pasul 9: Confirmă**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 207 verzi (201 + 5 + 1).

- [ ] **Pasul 10: Commit**

```bash
git add lib/core/display_name.dart lib/data/storage_service.dart lib/data/auth_service.dart test/display_name_test.dart test/account_reset_test.dart
git commit -m "Ordinea numelor intr-un singur loc, pura si testata"
```

---

## Sarcina 4: Oricine își poate schimba numele

**Fișiere:**
- Modifică: `lib/screens/profile_screen.dart` (`:121, 130, 256, 267, 286-288, 783, 796`)
- Modifică: `lib/screens/home_screen.dart` (`:103, 265, 474, 486`)
- Modifică: `lib/screens/multiplayer/multiplayer_screen.dart` (`:63-64, 190, 570`)
- Modifică: `lib/widgets/edit_name_dialog.dart`
- Modifică: `lib/screens/admin_screen.dart` (eticheta butonului)

**Interfețe:**
- Consumă: `StorageService.setChosenDisplayName` din Sarcina 3.

- [ ] **Pasul 1: Dialogul marchează alegerea**

În `lib/widgets/edit_name_dialog.dart`, înlocuiește linia `:65`:

```dart
  // înainte: await StorageService.setDisplayName(result);
  await StorageService.setChosenDisplayName(result);
```

Restul funcției rămâne neatins. În special `:62-64` — eliberarea lui
`forcedName` — e deja necondiționată și corectă pentru toată lumea;
restricția „doar Guest" trăia exclusiv în porțile din ecrane, pe care le
scoatem mai jos.

Actualizează documentația de la `:12-14` a funcției: `nameSetByAdmin` nu mai
descrie o unealtă de moderare, ci „ridică numele pus de admin înainte de
salvare, ca primul heartbeat să nu-l pună la loc".

- [ ] **Pasul 2: Scoate poarta din `ProfileScreen`**

În `lib/screens/profile_screen.dart`:

La `:121`, șterge câmpul `nameLocked` din construcția lui `_ProfileData`
(păstrează `nameSetByAdmin`, încă folosit de dialog). Șterge și comentariul
de la `:113-120`, care explică o regulă care nu mai există.

La `:130`, șterge garda: `if (data.nameLocked) return;`

La `:256`, `onTap` devine necondiționat:
```dart
                    onTap: () => _editName(data),
```

La `:267`, creionul se arată mereu:
```dart
                        const SizedBox(width: 8),
                        const Icon(Icons.edit_rounded, color: Colors.white54, size: 18),
```

La `:286-288`, textul devine unul singur:
```dart
                  child: Text(
                    tr('Apasă pe nume ca să-l schimbi', 'Tap your name to change it'),
                    style: const TextStyle(color: Colors.white38, fontSize: 11),
                  ),
```

La `:783` și `:796`, șterge câmpul `nameLocked` din clasa `_ProfileData` și
din constructorul ei.

- [ ] **Pasul 3: Scoate poarta din `HomeScreen`**

În `lib/screens/home_screen.dart`: șterge `nameLocked` de la `:103`, `:474`,
`:486` și comentariul de la `:100-102`. La `:265`:

```dart
            onNameTap: data == null ? null : () => _editName(data),
```

- [ ] **Pasul 4: Scoate poarta din `MultiplayerScreen`**

În `lib/screens/multiplayer/multiplayer_screen.dart` blocajul se numește
**`_isGoogleLinked`**, nu `nameLocked` — un grep după `nameLocked` nu-l
găsește, iar ecranul ar rămâne blocat.

La `:190`, șterge garda `if (_isGoogleLinked) return;`
La `:570`, scoate condiția `if (!_isGoogleLinked)` din jurul creionului.
La `:63-64`, șterge câmpul `_isGoogleLinked` dacă nu mai e folosit nicăieri
altundeva în fișier (verifică întâi cu grep — dacă mai are altă utilizare,
lasă-l).

- [ ] **Pasul 5: Redenumește butonul din Admin**

În `lib/screens/admin_screen.dart`, găsește butonul „Lasă-l liber" (apelează
`clearForcedNameAsAdmin`) și schimbă eticheta:

```dart
tr('Anulează redenumirea', 'Undo rename')
```

Funcția rămâne — e singura cale de a da înapoi fără să știi numele original
al jucătorului. Actualizează documentația lui `clearForcedNameAsAdmin`
(`lib/data/player_profile_service.dart:615-622`): nu mai ridică o „pedeapsă
pe viață", ci anulează o redenumire.

- [ ] **Pasul 6: Fă eliberarea numelui să nu blocheze fără net**

În `lib/data/player_profile_service.dart:652-664`, scrierea pe server nu se
mai așteaptă — offline, `set()` nu se încheie niciodată, iar dialogul ar
rămâne agățat. Partea locală se aplică imediat:

```dart
  Future<void> releaseMyForcedName() async {
    final uid = _uid;
    if (uid.isNotEmpty) {
      // Fără `await`: offline, scrierea nu se încheie până la reconectare, iar
      // dialogul de schimbare a numelui ar rămâne blocat. Firestore o ține în
      // coadă și o trimite singur. Eșecul se doar loghează.
      unawaited(_col.doc(uid).set({'forcedName': FieldValue.delete()}, SetOptions(merge: true)).catchError((e) {
        debugPrint('PlayerProfileService.releaseMyForcedName a esuat: $e');
      }));
    }
    await StorageService.setForcedName('');
  }
```

Adaugă `import 'dart:async';` dacă lipsește.

- [ ] **Pasul 7: Confirmă**

Rulează: `flutter analyze` → „No issues found!" (prinde orice `nameLocked`
sau `_isGoogleLinked` rămas).
Rulează: `flutter test` → 207 verzi.

- [ ] **Pasul 8: Commit**

```bash
git add lib/screens lib/widgets/edit_name_dialog.dart lib/data/player_profile_service.dart
git commit -m "Oricine isi poate schimba numele; redenumirea din Admin nu mai blocheaza"
```

---

## Sarcina 5: Coordonatorul de abonamente + resursele de la admin

**Fișiere:**
- Creează: `lib/data/live_sync.dart`
- Modifică: `lib/data/cloud_sync_service.dart` (`startLive`/`stopLive`)
- Modifică: `lib/main.dart` (`initState`, `didChangeAppLifecycleState`)

**Interfețe:**
- Produce: `LiveSync.instance.start()` / `LiveSync.instance.stop()` —
  pornesc și opresc toate abonamentele deodată.
- Produce: `LiveSync.instance.attachToIdentity()` — se cheamă o dată, din
  `initState`; se leagă singur de `FirebaseAuth.userChanges()`.
- Consumat de: Sarcinile 6-9, care își adaugă serviciul în `start`/`stop`.

- [ ] **Pasul 1: Scrie coordonatorul**

Creează `lib/data/live_sync.dart`:

```dart
import 'package:firebase_auth/firebase_auth.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:flutter/foundation.dart';
import 'cloud_sync_service.dart';

/// Pornește și oprește, dintr-un singur loc, toate abonamentele care aduc
/// schimbări din exterior cât timp jocul e deschis (resurse de la admin,
/// redenumire, anunțuri, blocări, cereri de prietenie, mesaje).
///
/// DE CE E LEGAT DE IDENTITATE, NU DOAR DE CICLUL DE VIAȚĂ: un abonament
/// pornit pentru un uid și rămas deschis după ce identitatea s-a schimbat ar
/// livra datele altcuiva. Căile prin care uid-ul se schimbă sunt mai multe
/// decât pare — legare Google, legare Play Games, `credential-already-in-use`
/// (uid nou), delogare (uid GOL, fără identitate nouă până la repornire) și
/// ștergere de cont (șterge profilul cu uid-ul vechi ÎNCĂ activ). De aceea
/// [_onIdentityChanged] anulează tot NECONDIȚIONAT, înainte de orice
/// verificare, și abia apoi decide dacă repornește.
///
/// `userChanges()`, nu `authStateChanges()`: la legarea unui cont Google
/// peste identitatea anonimă uid-ul NU se schimbă, deci `authStateChanges`
/// poate să nu emită deloc — dar numele afișat se schimbă, iar de el depinde
/// ce se vede în joc.
class LiveSync {
  LiveSync._();
  static final instance = LiveSync._();

  StreamSubscription<User?>? _identitySub;
  String? _startedForUid;
  bool _running = false;

  /// Se cheamă O SINGURĂ DATĂ, din `_GuessItAppState.initState`. Flutter nu
  /// livrează starea inițială de ciclu de viață prin
  /// `didChangeAppLifecycleState`, deci prima pornire nu poate veni de acolo.
  void attachToIdentity() {
    // Fără gardă, `test/widget_test.dart` — care montează aplicația direct,
    // fără `Firebase.initializeApp` — ar arunca la simpla atingere a lui
    // FirebaseAuth.instance. Vezi precedentul din main.dart.
    if (Firebase.apps.isEmpty) return;
    try {
      _identitySub = FirebaseAuth.instance.userChanges().listen(_onIdentityChanged);
    } catch (e) {
      debugPrint('LiveSync.attachToIdentity a esuat: $e');
    }
  }

  void _onIdentityChanged(User? user) {
    final uid = user?.uid ?? '';
    // Un eveniment care nu schimbă uid-ul (ex. `updateProfile` după legarea
    // unui cont Google) nu are de ce să reconstruiască abonamentele.
    if (_running && uid.isNotEmpty && uid == _startedForUid) return;
    stop();
    if (uid.isEmpty) return;
    _startedForUid = uid;
    start();
  }

  /// Pornește abonamentele. Sigur de chemat de mai multe ori.
  void start() {
    if (_running) return;
    _running = true;
    CloudSyncService.instance.startLive();
  }

  /// Le oprește pe toate. Sigur de chemat oricând, inclusiv când nu rulează
  /// nimic — se cheamă și la trecerea în fundal, și la fiecare schimbare de
  /// identitate.
  void stop() {
    if (!_running) return;
    _running = false;
    CloudSyncService.instance.stopLive();
  }
}
```

Adaugă `import 'dart:async';` în capul fișierului.

- [ ] **Pasul 2: Abonamentul pe cutia poștală de la admin**

În `lib/data/cloud_sync_service.dart`, adaugă în clasă:

```dart
  StreamSubscription<DocumentSnapshot<Map<String, dynamic>>>? _grantSub;

  /// Ascultă cutia poștală a acestui cont, ca resursele trimise de admin să
  /// intre în cont CÂT timp jucătorul e în joc — nu abia la următoarea
  /// pornire, cum era înainte.
  ///
  /// Se ignoră două feluri de snapshot-uri, amândouă produse de noi înșine:
  /// cele cu scrieri locale în așteptare, și cele în care documentul nu mai
  /// există — tranzacția de revendicare din [consumePendingGrant] tocmai l-a
  /// șters, deci ascultătorul se aprinde imediat după fiecare consum.
  void startLive() {
    final uid = _uid;
    if (uid.isEmpty) return;
    stopLive();
    try {
      _grantSub = FirebaseFirestore.instance
          .collection('admin_grants')
          .doc(uid)
          .snapshots()
          .listen((snap) {
        if (snap.metadata.hasPendingWrites) return;
        if (!snap.exists) return;
        consumePendingGrant();
      });
    } catch (e) {
      debugPrint('CloudSyncService.startLive a esuat: $e');
    }
  }

  void stopLive() {
    _grantSub?.cancel();
    _grantSub = null;
  }
```

Adaugă `import 'dart:async';`.

- [ ] **Pasul 3: Leagă în `main.dart`**

În `lib/main.dart`, în `_GuessItAppState.initState` (`:106-112`), după
`_listenForMultiplayerPresence();`:

```dart
    LiveSync.instance.attachToIdentity();
```

În `didChangeAppLifecycleState` (`:353-368`):

```dart
  @override
  void didChangeAppLifecycleState(AppLifecycleState state) {
    if (state == AppLifecycleState.paused ||
        state == AppLifecycleState.detached ||
        // Pe web `paused` poate să nu vină niciodată — se primește `hidden`.
        // Verificarea cu doi jucători rulează în Chrome, deci fără ramura
        // asta abonamentele n-ar fi oprite niciodată chiar acolo unde se
        // măsoară costul lor.
        state == AppLifecycleState.hidden) {
      LiveSync.instance.stop();
      CloudSyncService.instance.push();
      Music.pauseForBackground();
    } else if (state == AppLifecycleState.resumed) {
      LiveSync.instance.start();
      Music.resumeFromBackground();
      PlayerProfileService.instance.ensureProfileHeartbeat();
      // `consumePendingGrant` NU se mai cheamă aici: abonamentul din LiveSync
      // îl declanșează singur, iar reatașarea aduce oricum un snapshot
      // proaspăt cu tot ce s-a schimbat cât aplicația era în fundal.
      ModerationService.instance.loadBlocked();
      NotificationService.instance.pullFromCloud().then((_) {
        return NotificationService.instance.refreshUnread();
      });
    }
  }
```

Adaugă `import 'data/live_sync.dart';`.

- [ ] **Pasul 4: Confirmă**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 207 verzi. **`test/widget_test.dart` trebuie să
treacă** — e dovada că garda `Firebase.apps.isEmpty` funcționează.

- [ ] **Pasul 5: Commit**

```bash
git add lib/data/live_sync.dart lib/data/cloud_sync_service.dart lib/main.dart
git commit -m "Coordonator de abonamente live; resursele de la admin ajung instant"
```

---

## Sarcina 6: Redenumirea ajunge instant

**Fișiere:**
- Modifică: `lib/data/player_profile_service.dart` (`startLive`/`stopLive`)
- Modifică: `lib/data/live_sync.dart` (înregistrează serviciul)

**Interfețe:**
- Consumă: `LiveSync.start()`/`stop()` din Sarcina 5.

- [ ] **Pasul 1: Abonamentul pe propriul profil**

În `lib/data/player_profile_service.dart`:

```dart
  StreamSubscription<DocumentSnapshot<Map<String, dynamic>>>? _profileSub;

  /// Ascultă propriul profil public, ca o redenumire făcută din panoul de
  /// Admin să apară pe ecran imediat, nu la următoarea pornire.
  ///
  /// CAPCANA: documentul ăsta e chiar cel în care scrie
  /// [ensureProfileHeartbeat], deci ascultătorul se aprinde și la scrierile
  /// noastre. Se ignoră snapshot-urile cu scrieri locale în așteptare, iar
  /// `forcedName` se compară cu valoarea locală înainte de orice scriere —
  /// fără ambele, se închide bucla scriere → notificare → scriere, care ar
  /// consuma cotă Firestore cu aplicația nefolosită.
  ///
  /// Comparația e în AMBELE sensuri, ca și în heartbeat: dacă adminul anulează
  /// redenumirea, câmpul dispare de pe server și trebuie șters și local —
  /// altfel jucătorul ar rămâne cu numele primit fără ca nimeni să-l poată
  /// anunța că e din nou liber.
  void startLive() {
    final uid = _uid;
    if (uid.isEmpty) return;
    stopLive();
    try {
      _profileSub = _col.doc(uid).snapshots().listen((snap) async {
        if (snap.metadata.hasPendingWrites) return;
        final forced = snap.data()?['forcedName'] as String? ?? '';
        if (forced == await StorageService.getForcedName()) return;
        await StorageService.setForcedName(forced);
        profileChanged.value++;
      });
    } catch (e) {
      debugPrint('PlayerProfileService.startLive a esuat: $e');
    }
  }

  void stopLive() {
    _profileSub?.cancel();
    _profileSub = null;
  }

  /// Crește când numele afișat s-a schimbat din exterior. Ecranele care arată
  /// numele ascultă asta, la fel cum cele cu resurse ascultă
  /// StorageService.balanceRevision.
  final ValueNotifier<int> profileChanged = ValueNotifier<int>(0);
```

- [ ] **Pasul 2: Ecranele cu nume ascultă**

În `lib/screens/profile_screen.dart`, `lib/screens/home_screen.dart` și
`lib/screens/multiplayer/multiplayer_screen.dart`, adaugă alături de
ascultătorul de balanță din Sarcina 2:

```dart
    PlayerProfileService.instance.profileChanged.addListener(_refresh);
```

și, în `dispose`:

```dart
    PlayerProfileService.instance.profileChanged.removeListener(_refresh);
```

- [ ] **Pasul 3: Înregistrează în coordonator**

În `lib/data/live_sync.dart`, în `start()` și `stop()`:

```dart
    PlayerProfileService.instance.startLive();
```
```dart
    PlayerProfileService.instance.stopLive();
```

- [ ] **Pasul 4: Confirmă**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 207 verzi.

- [ ] **Pasul 5: Commit**

```bash
git add lib/data/player_profile_service.dart lib/data/live_sync.dart lib/screens
git commit -m "Redenumirea din Admin apare pe loc pe ecranul jucatorului"
```

---

## Sarcina 7: Anunțurile de la admin aprind bulina pe loc

**Fișiere:**
- Modifică: `lib/data/notification_service.dart`
- Modifică: `lib/data/live_sync.dart`

- [ ] **Pasul 1: Gardă de reintrare pentru `pullFromCloud`**

`consumePendingGrant` are `_consumingGrant` exact din acest motiv; sub un
declanșator live, două rulări concurente ale lui `pullFromCloud` pot
**pierde o notificare** — `addLocal` (`:74-80`) e citire-modificare-scriere,
deci ambele citesc aceeași listă și fiecare o scrie pe a ei.

În `lib/data/notification_service.dart`, în clasă:

```dart
  /// Vezi CloudSyncService._consumingGrant — același tipar, același motiv.
  /// Fără ea, două descărcări suprapuse pot pierde o notificare: [addLocal]
  /// citește lista, o modifică și o scrie înapoi, deci a doua rulare ar
  /// suprascrie ce tocmai a adăugat prima.
  bool _pulling = false;
```

Și în `pullFromCloud` (`:154`), la început și în `finally`:

```dart
  Future<void> pullFromCloud() async {
    if (_pulling) return;
    _pulling = true;
    try {
      // ... corpul existent, neschimbat ...
    } finally {
      _pulling = false;
    }
  }
```

- [ ] **Pasul 2: Abonamentul pe cutia poștală de anunțuri**

```dart
  StreamSubscription<QuerySnapshot<Map<String, dynamic>>>? _inboxSub;

  /// Anunțurile lăsate de admin ajung acum cât jucătorul e în joc.
  ///
  /// Se cheamă [pullFromCloud], nu se procesează snapshot-ul direct: aceea
  /// știe deja să copieze anunțurile local ȘI să le șteargă din cloud, iar
  /// duplicarea acelei logici aici ar fi însemnat două căi care trebuie ținute
  /// în sincron. Garda `_pulling` face apelurile suprapuse inofensive.
  void startLive() {
    final uid = _uid;
    if (uid.isEmpty) return;
    stopLive();
    try {
      _inboxSub = _cloudBox(uid).snapshots().listen((snap) {
        if (snap.metadata.hasPendingWrites) return;
        if (snap.docs.isEmpty) return;
        pullFromCloud().then((_) => refreshUnreadLocalOnly());
      });
    } catch (e) {
      debugPrint('NotificationService.startLive a esuat: $e');
    }
  }

  void stopLive() {
    _inboxSub?.cancel();
    _inboxSub = null;
  }
```

Se cheamă `refreshUnreadLocalOnly()`, nu `refreshUnread()`: al doilea
declanșează `fetchLive()`, adică `2 + N` citiri Firestore (N = numărul de
prieteni) pentru un singur anunț sosit. Anunțurile sunt deja salvate local în
acel moment, deci partea locală ajunge.

- [ ] **Pasul 3: Înregistrează în coordonator**

În `lib/data/live_sync.dart`, în `start()`/`stop()`:

```dart
    NotificationService.instance.startLive();
```
```dart
    NotificationService.instance.stopLive();
```

- [ ] **Pasul 4: Confirmă**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 207 verzi.

- [ ] **Pasul 5: Commit**

```bash
git add lib/data/notification_service.dart lib/data/live_sync.dart
git commit -m "Anunturile de la admin aprind bulina fara repornire"
```

---

## Sarcina 8: Blocările se aliniază în cadrul sesiunii

**Fișiere:**
- Modifică: `lib/data/moderation_service.dart`
- Modifică: `lib/data/live_sync.dart`

Notă: ieșirea devreme din `loadBlocked` (`:56`) **nu e un bug** — e
comportament documentat intenționat la `:49-52`, cu portița `force: true`.
Golul real e mai îngust: o blocare făcută de pe alt telefon nu ajunge aici în
cadrul sesiunii. Abonamentul îl închide.

- [ ] **Pasul 1: Abonamentul**

În `lib/data/moderation_service.dart`:

```dart
  StreamSubscription<QuerySnapshot<Map<String, dynamic>>>? _blockedSub;

  /// Ține lista de blocați aliniată cu serverul cât timp jocul e deschis —
  /// altfel o blocare făcută de pe alt telefon, cu același cont, s-ar aplica
  /// abia la următoarea pornire (vezi [loadBlocked], care iese devreme
  /// intenționat când lista e deja încărcată pentru uid-ul curent).
  void startLive() {
    final me = _uid;
    if (me.isEmpty) return;
    stopLive();
    try {
      _blockedSub = _blockedCol(me).snapshots().listen((snap) {
        blockedIds.value = snap.docs.map((d) => d.id).toSet();
      });
    } catch (e) {
      debugPrint('ModerationService.startLive a esuat: $e');
    }
  }

  void stopLive() {
    _blockedSub?.cancel();
    _blockedSub = null;
  }
```

- [ ] **Pasul 2: Înregistrează în coordonator**

```dart
    ModerationService.instance.startLive();
```
```dart
    ModerationService.instance.stopLive();
```

- [ ] **Pasul 3: Confirmă și commit**

Rulează: `flutter analyze`, `flutter test` → 207 verzi.

```bash
git add lib/data/moderation_service.dart lib/data/live_sync.dart
git commit -m "Blocarile se aliniaza in cadrul sesiunii"
```

---

## Sarcina 9: Mesajele și cererile de prietenie, live

**Fișiere:**
- Modifică: `lib/data/friend_chat_service.dart`
- Modifică: `lib/data/player_profile_service.dart` (cereri)
- Modifică: `lib/data/notification_service.dart` (`unreadCount` din snapshot)
- Modifică: `lib/screens/friends_screen.dart`
- Modifică: `lib/data/live_sync.dart`

**ATENȚIE — de ce NU se interoghează colecția.** Tentația e
`friend_chats.where('members', arrayContains: uid)`. Ar pica cu
`permission-denied`: regula (`firestore.rules:346-351`) verifică apartenența
despicând NUMELE documentului, iar o interogare pe colecție nu-i dă motorului
nicio cale s-o demonstreze — regulile autorizează, nu filtrează. Ar mai avea
două defecte: `markRead` (`friend_chat_service.dart:107-109`) creează
documentul fără câmpul `members`, deci firele deschise dar netrimise ar lipsi
din rezultate; iar sortarea după `lastMessageAt` ar cere un index compus creat
manual, pe care repo-ul nu-l are.

**În loc:** câte un abonament pe documentul fiecărui fir. `threadId` e fixat,
deci regula existentă le acoperă textual. Zero reguli noi, zero indexuri,
același cost ca cele N `get()` de azi.

- [ ] **Pasul 1: Un flux per fir**

În `lib/data/friend_chat_service.dart`:

```dart
  /// Rezumatul unui fir anume, live. Un abonament pe DOCUMENT, nu o
  /// interogare pe colecție — vezi nota de mai sus pentru de ce interogarea
  /// ar fi respinsă de reguli.
  ///
  /// Întoarce același `FriendChatSummary` pe care îl dă [fetchSummaries]
  /// (`lib/models/friend_chat.dart:30`), ca ecranul de Prieteni să nu aibă
  /// două forme de rezumat de ținut în sincron.
  Stream<FriendChatSummary?> watchSummary(String otherUid) {
    final threadId = _myThreadWith(otherUid);
    if (threadId == null) return Stream.value(null);
    return _thread(threadId).snapshots().map(
          (snap) => snap.exists ? FriendChatSummary.fromDoc(snap) : null,
        );
  }
```

`FriendChatSummary.fromDoc` există deja la `lib/models/friend_chat.dart:48`,
iar `hasUnreadFor(String uid)` la `:65` — nu se scrie logică nouă de
„necitit", se refolosește cea existentă.

- [ ] **Pasul 2: Cererile de prietenie, live**

În `lib/data/player_profile_service.dart`:

```dart
  /// Cererile primite, live. E o subcolecție sub `player_profiles/{uid}`,
  /// deci wildcard-ul părinte e fixat de calea interogării și regula
  /// existentă (`firestore.rules:183`) o acoperă — `fetchIncomingRequests`
  /// face deja exact acest `list` azi.
  Stream<int> watchPendingRequestCount() {
    final uid = _uid;
    if (uid.isEmpty) return Stream.value(0);
    return _requestsCol(uid).snapshots().map((s) => s.docs.length);
  }
```

- [ ] **Pasul 3: Bulina se actualizează din snapshot, nu prin recitire**

**Condiție de acceptare, nu detaliu.** Dacă fiecare eveniment ajunge pe
`refreshUnread()` → `fetchLive()`, un singur mesaj primit costă `2 + N`
citiri Firestore (N = numărul de prieteni) — adică bulina „live" ar fi mai
scumpă decât poll-ul pe care îl înlocuiește.

În `lib/data/notification_service.dart`:

```dart
  /// Partea live a bulinei, ținută de abonamente în loc să fie recitită.
  /// Vezi [refreshUnread] pentru varianta care chiar întreabă rețeaua — aceea
  /// rămâne pentru deschiderea panoului, unde oricum se citește tot.
  final ValueNotifier<int> _liveUnread = ValueNotifier<int>(0);

  /// Anunțat de abonamentele din LiveSync: câte cereri în așteptare și câte
  /// fire cu mesaj necitit există ACUM. Nu produce nicio citire în plus —
  /// cifrele vin din snapshot-urile deja primite.
  void setLiveUnread({required int pendingRequests, required int unreadThreads}) {
    _liveUnread.value = pendingRequests + unreadThreads;
    _recomputeUnread();
  }

  Future<void> _recomputeUnread() async {
    try {
      final readAt = await StorageService.getNotificationsReadAt();
      final stored = await loadStored();
      final unreadStored = stored.where((n) => n.createdAt.millisecondsSinceEpoch > readAt).length;
      unreadCount.value = unreadStored + _liveUnread.value;
    } catch (e) {
      debugPrint('NotificationService._recomputeUnread a esuat: $e');
    }
  }
```

Widget-ul clopoțelului (`lib/widgets/notification_bell.dart`) **nu se
atinge**: e deja un `ValueListenableBuilder` pe `unreadCount` (`:63-64`) și e
corect. Îi lipsea doar sursa.

- [ ] **Pasul 4: Leagă abonamentele în coordonator**

În `lib/data/live_sync.dart`, `start()` pornește un abonament pe cereri și
câte unul pe firul fiecărui prieten. Lista de prieteni se citește o dată la
pornire; când se schimbă (prieten adăugat/șters), abonamentele se refac.

```dart
  StreamSubscription<int>? _requestsSub;
  final List<StreamSubscription<dynamic>> _threadSubs = [];
  int _pendingRequests = 0;
  final Set<String> _unreadThreads = {};

  Future<void> _startFriendWatchers() async {
    _requestsSub = PlayerProfileService.instance.watchPendingRequestCount().listen((count) {
      _pendingRequests = count;
      _pushUnread();
    });
    // fetchFriends() -> List<PlayerProfile> (player_profile_service.dart:451)
    final friends = await PlayerProfileService.instance.fetchFriends();
    final me = MultiplayerService.instance.currentPlayerId;
    for (final friend in friends) {
      _threadSubs.add(
        FriendChatService.instance.watchSummary(friend.uid).listen((summary) {
          // hasUnreadFor e deja scrisa si testata (friend_chat.dart:65) —
          // stie ca lipsa lui readAt[me] NU inseamna „citit".
          if (summary != null && summary.hasUnreadFor(me)) {
            _unreadThreads.add(friend.uid);
          } else {
            _unreadThreads.remove(friend.uid);
          }
          _pushUnread();
        }),
      );
    }
  }

  void _pushUnread() => NotificationService.instance.setLiveUnread(
        pendingRequests: _pendingRequests,
        unreadThreads: _unreadThreads.length,
      );
```

Importuri necesare în `live_sync.dart`: `friend_chat_service.dart`,
`player_profile_service.dart`, `notification_service.dart`,
`multiplayer_service.dart`.

În `stop()`:

```dart
    _requestsSub?.cancel();
    _requestsSub = null;
    for (final sub in _threadSubs) {
      sub.cancel();
    }
    _threadSubs.clear();
    _unreadThreads.clear();
    _pendingRequests = 0;
```

- [ ] **Pasul 5: Ecranul de Prieteni se reîmprospătează**

Ecranul are deja `_reload()` (`friends_screen.dart:60-63`), care reface
`_dataFuture`. Nu se rescrie nimic — se leagă doar de semnalul live.

În `_FriendsScreenState`:

```dart
  @override
  void initState() {
    super.initState();
    // Ecranul era o poză făcută la intrare: stăteai cu el deschis, prietenul
    // îți scria, și nu apărea nimic. Abonamentele din LiveSync mișcă bulina
    // globală; când se mișcă, rândurile de aici se recitesc.
    NotificationService.instance.unreadCount.addListener(_reloadFromLive);
  }

  @override
  void dispose() {
    NotificationService.instance.unreadCount.removeListener(_reloadFromLive);
    _codeController.dispose();
    super.dispose();
  }

  void _reloadFromLive() {
    if (!mounted) return;
    setState(() => _dataFuture = _load());
  }
```

Verifică dacă `dispose` există deja în clasă (`_codeController` trebuie
eliberat oricum) — dacă da, adaugă doar linia de `removeListener` în el, nu
crea un al doilea `dispose`.

- [ ] **Pasul 6: Confirmă și commit**

Rulează: `flutter analyze`, `flutter test` → 207 verzi.

```bash
git add lib/data lib/screens/friends_screen.dart
git commit -m "Mesajele si cererile de prietenie ajung live, fara citiri in plus"
```

---

## Sarcina 10: Ban-ul devine real

**Fișiere:**
- Modifică: `firestore.rules`
- Modifică: `lib/data/player_profile_service.dart`
- Modifică: `lib/screens/multiplayer/multiplayer_screen.dart`

Azi `banned_players` e scrisă (`player_profile_service.dart:672`) și
**niciodată citită** de aplicație: cel banat joacă mai departe, îi dispar doar
prietenii și clasamentul, iar scrierile lui eșuează în tăcere.

Decizie: **află și nu mai poate juca online.** Multiplayer și clasament
închise, cu mesaj clar. Single-player rămâne — un ban greșit nu ia omului tot
jocul.

- [ ] **Pasul 1: Regula nouă**

În `firestore.rules`, la `match /banned_players/{uid}` (`:371-372`):

```
    match /banned_players/{uid} {
      // Proprietarul poate citi DOAR propriul document, ca aplicatia sa stie
      // sa-i spuna de ce nu mai poate intra in multiplayer. Nu poate scrie
      // nimic: un ban nu se ridica singur.
      allow read: if request.auth != null && request.auth.uid == uid;
      allow read, write: if request.auth != null && request.auth.token.email == 'dragosssx@gmail.com';
    }
```

- [ ] **Pasul 2: Verifică regula ÎNAINTE de deploy**

Deschide Firebase Console → Firestore → Rules → **Rules Playground**.
Simulează: `get` pe `/databases/(default)/documents/banned_players/UID_TEST`,
autentificat ca `UID_TEST` → trebuie **Allow**. Apoi ca alt uid → trebuie
**Deny**.

Nu trece mai departe fără ambele rezultate. Deploy-ul cere OK explicit de la
proprietar.

- [ ] **Pasul 3: Citește starea de ban**

În `lib/data/player_profile_service.dart`:

```dart
  /// Sunt banat? Ascultat live, ca ridicarea unui ban să ajungă la fel de
  /// repede ca punerea lui.
  final ValueNotifier<bool> amIBanned = ValueNotifier<bool>(false);

  StreamSubscription<DocumentSnapshot<Map<String, dynamic>>>? _banSub;

  void startBanWatch() {
    final uid = _uid;
    if (uid.isEmpty) return;
    stopBanWatch();
    try {
      _banSub = _db.collection('banned_players').doc(uid).snapshots().listen((snap) {
        amIBanned.value = snap.exists;
      });
    } catch (e) {
      debugPrint('PlayerProfileService.startBanWatch a esuat: $e');
    }
  }

  void stopBanWatch() {
    _banSub?.cancel();
    _banSub = null;
  }
```

Înregistrează în `LiveSync.start()`/`stop()`.

- [ ] **Pasul 4: Închide multiplayer-ul pentru cel banat**

În `lib/screens/multiplayer/multiplayer_screen.dart`, la intrare, dacă
`PlayerProfileService.instance.amIBanned.value` e adevărat, arată în locul
conținutului:

```dart
tr('Contul tău a fost restricționat de administrator. Nu poți juca online.',
   'Your account has been restricted by an administrator. You cannot play online.')
```

Single-player rămâne accesibil.

- [ ] **Pasul 5: Confirmă și commit**

Rulează: `flutter analyze`, `flutter test` → 207 verzi.

```bash
git add firestore.rules lib/data/player_profile_service.dart lib/screens/multiplayer/multiplayer_screen.dart lib/data/live_sync.dart
git commit -m "Ban-ul chiar inchide multiplayer-ul, si jucatorul afla de ce"
```

---

## Sarcina 11: Curățenie — cod mort și duplicare

**Fișiere:**
- Modifică: `lib/data/storage_service.dart`, `lib/core/ads_service.dart`
- Creează: `lib/core/powerup_ui.dart`
- Modifică: `lib/screens/multiplayer/multiplayer_electric_chair_screen.dart`,
  `lib/screens/multiplayer/multiplayer_obby_screen.dart`,
  `lib/screens/multiplayer/multiplayer_tanks_screen.dart`

Găsit de un audit dedicat. Trei zone au ieșit **curate** și nu se ating:
toate cele 50 de chei de preferințe sunt folosite, curățenia după „Modul Eco"
și după teme a fost completă, iar toate cele 22 de colecții Firestore sunt și
scrise și citite.

**Capcană de metodă, obligatorie:** `grep` clasifică
`lib/screens/admin_screen.dart` drept fișier **binar** și îl sare tăcut. Un
sweep de cod mort fără `-a` produce fals-pozitive — două metode păreau moarte
deși sunt chemate chiar de acolo. Folosește `grep -a` la orice verificare de
„mai e chemat?".

- [ ] **Pasul 1: Confirmă că cele 8 metode chiar sunt moarte**

```bash
for m in getLastClaimedRewardLevel resetProgress canPlayDailyChallenge \
         getLeaderboardPoints getHintsUsedTotal getQuestsClaimedTotal \
         getDailyChallengesTotal isRewardedReady; do
  echo "=== $m: $(grep -ra "$m" lib/ test/ | wc -l) aparitii"
done
```

Așteptat: fiecare cu **1** apariție (doar definiția). Orice cu 2+ NU se
șterge — verifică unde e chemată.

- [ ] **Pasul 2: Șterge-le**

| Fișier:linie | Metodă |
|---|---|
| `lib/data/storage_service.dart:528` | `getLastClaimedRewardLevel()` |
| `lib/data/storage_service.dart:587` | `resetProgress()` |
| `lib/data/storage_service.dart:767` | `canPlayDailyChallenge()` |
| `lib/data/storage_service.dart:1502` | `getLeaderboardPoints()` |
| `lib/data/storage_service.dart:1529` | `getHintsUsedTotal()` |
| `lib/data/storage_service.dart:1539` | `getQuestsClaimedTotal()` |
| `lib/data/storage_service.dart:1544` | `getDailyChallengesTotal()` |
| `lib/core/ads_service.dart:120` | `bool get isRewardedReady` |

Cele patru învelișuri (`getLastClaimedRewardLevel`, `getHintsUsedTotal`,
`getQuestsClaimedTotal`, `getDailyChallengesTotal`) se șterg **fără să
atingă datele**: cheile lor se citesc direct în altă parte
(`storage_service.dart:538, 556, 1596, 1597, 1598`). Nu șterge cheile.

- [ ] **Pasul 3: Confirmă că nu s-a rupt nimic**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 207 verzi.

- [ ] **Pasul 4: Commit ștergerea, separat de refactorizare**

```bash
git add lib/data/storage_service.dart lib/core/ads_service.dart
git commit -m "Sterge 8 metode pe care nu le mai cheama nimeni"
```

- [ ] **Pasul 5: Scoate blocul de power-up duplicat**

Același bloc de ~45 de linii e copiat de trei ori, identic caracter cu
caracter în afară de numele unei variabile locale:

- `multiplayer_electric_chair_screen.dart:254-297`
- `multiplayer_obby_screen.dart:213-243` și `:275`
- `multiplayer_tanks_screen.dart:311-356`

Creează `lib/core/powerup_ui.dart` cu cele trei funcții, primind ca parametri
exact ce diferă între copii: id-ul meu, harta de nume, și contextul.

```dart
/// Bucățile de interfață pentru power-up-uri, comune tuturor modurilor
/// multiplayer. Erau copiate identic în Scaunul Electric, Obby și Tanks —
/// o schimbare la una însemna trei locuri de ținut minte, iar al treilea se
/// uita.
///
/// Se transmit ca parametri exact lucrurile care difereau între copii: id-ul
/// jucătorului curent și harta de nume. Restul era identic.
void showPeekResults(BuildContext context, {
  required Map<String, String> answers,
  required Map<String, String> playerNames,
}) { /* corpul comun, mutat aici */ }

void notifyPowerUpTooLate(BuildContext context) { /* ... */ }

void announcePowerUp(BuildContext context, {
  required String powerUpId,
  required String fromName,
}) { /* ... */ }
```

Înlocuiește cele trei copii cu apeluri. **Verifică prin `diff` că blocul
mutat e chiar identic** înainte să ștergi copiile — dacă vreuna a divergat pe
parcurs, diferența e intenționată și trebuie păstrată ca parametru.

- [ ] **Pasul 6: Confirmă**

Rulează: `flutter analyze` → „No issues found!"
Rulează: `flutter test` → 207 verzi.

Cele trei ecrane ating tranzacția de rezolvare a rundei, deci refactorizarea
lor **trebuie probată cu jucători reali** la Sarcina 12 — testele automate nu
acoperă zona asta.

- [ ] **Pasul 7: Commit**

```bash
git add lib/core/powerup_ui.dart lib/screens/multiplayer
git commit -m "Un singur loc pentru interfata de power-up, in loc de trei copii"
```

**Rămas neverificat, notat cinstit pentru mai târziu:** auditul n-a apucat să
treacă prin `lib/screens/**` (~100 de fișiere) și `lib/widgets/**` după
widget-uri și funcții top-level nefolosite, nici prin assets moarte
(imagini/sunete la care nu se referă niciun cod). Acolo poate mai fi pradă;
nu intră în planul ăsta.

---

## Sarcina 12: Verificare pe viu, cu doi jucători

Nimic din planul ăsta nu se declară gata pe baza testelor automate:
abonamentele nu pot fi probate decât cu Firestore real.

- [ ] **Pasul 1: Build web curat**

```bash
flutter build web --release
```

Capcană cunoscută: un `.dart_tool` vechi produce un build **fără plugin-uri**
(Firebase mort), fără nicio eroare. Verifică prezența plugin-urilor în
build-ul ieșit înainte să tragi vreo concluzie dintr-un test picat.

- [ ] **Pasul 2: Doi jucători + Admin**

Playwright, două contexte Chrome separate = doi jucători reali. Al treilea
tab: panoul de Admin al aplicației, logat cu contul de proprietar.

- [ ] **Pasul 3: Rulează cele șapte verificări**

1. Jucătorul stă în meniu; adminul îi trimite monede → cifra se schimbă fără
   ca nimeni să atingă ceva.
2. Același test cu jucătorul pe **Profil** și pe **Magazin** — a doua
   jumătate a bugului.
3. Redenumire din Admin → apare instant; apoi jucătorul și-l schimbă singur,
   **inclusiv pe cont Google**.
4. „Anulează redenumirea" → se întoarce la numele lui.
5. Anunț din Admin → bulina se aprinde fără repornire.
6. Ban → primește mesajul, nu mai intră în multiplayer; ridicarea îl lasă
   înapoi.
7. **Cost:** aplicația stă deschisă și neatinsă două minute → contorul de
   scrieri din consola Firestore **nu crește**. Un mesaj primit produce **o**
   citire, nu `2 + N`.

Pentru fiecare: captură de ecran ca dovadă. O verificare fără dovadă nu e
făcută.

- [ ] **Pasul 4: Pe telefon**

```bash
flutter build apk --release
```

Verifică data fișierului APK înainte de instalare — `flutter install` poate
reinstala tăcut un APK vechi.

```bash
ADB="/c/Users/drago/AppData/Local/Android/Sdk/platform-tools/adb.exe"
MSYS_NO_PATHCONV=1 "$ADB" install -d build/app/outputs/flutter-apk/app-release.apk
```

Fără `-r`: instalare clean.

Repetă verificările 1, 3 și 5 pe telefon, cu Admin-ul în browser.

- [ ] **Pasul 5: Raportează ce a mers și ce nu**

Fără rotunjiri. Orice verificare picată se raportează cu dovada ei, nu se
reîncearcă până iese.
