<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Notificari pe telefon (vezi lib/data/device_notification_service.dart).
         POST_NOTIFICATIONS e ceruta de la Android 13 incolo si trebuie ceruta
         SI la rulare, nu doar declarata aici.
         SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM: notificarile noastre sunt
         legate de momente exacte (roata s-a resetat la fix 24h de la ultimul
         spin, planeta la fix 12h). Fara ele, Android amana declansarea cu
         zeci de minute ca sa economiseasca bateria, iar "e gata acum" ar
         ajunge cand nu mai e adevarat.
         RECEIVE_BOOT_COMPLETED: alarmele programate se pierd la repornirea
         telefonului; cu ea, pluginul le reprogrameaza singur. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:label="SodoQuizz"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Link de invitație prieteni: guessit://addfriend/<cod> — schemă
                 proprie, nu Android App Link, ca să nu ceară assetlinks.json
                 găzduit pe GH Pages (vezi docs/deploy). Ținta e citită în
                 main.dart, GuessItApp._listenForDeepLinks, cu pachetul app_links. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="guessit" android:host="addfriend"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- IMPELLER PORNIT — retestat 2026-09-01, bug-ul nu se mai reproduce.
             ISTORIC: renderer-ul Impeller (Vulkan) a fost OPRIT aici o
             perioadă, din cauza unui ecran negru după login-ul Google.
             Simptom: după ce alegeai contul în fereastra nativă Google,
             ecranul rămânea complet negru dar muzica mergea (deci aplicația
             trăia, doar desenul era mort); doar o repornire completă îl
             recupera. Cauza: pe GPU-uri Samsung Xclipse, Impeller pierdea
             suprafața de desen când aplicația era readusă în față de o
             Activitate externă — iar fereastra Google e exact asta.
             RETESTAT pe Samsung SM-S908B (Xclipse), Flutter 3.27.4: login
             Google complet + ciclu fundal/prim-plan, fără niciun ecran negru
             și fără nicio eroare Impeller/Vulkan în logcat. Reactivat.
             DACĂ REAPARE: pune la loc pe "false" — un ecran negru fără niciun
             mesaj e mult mai rău decât o pierdere mică de fps. Simptomul e
             insidios (logcat curat, nicio excepție), deci nu-l căuta în codul
             Dart, nu e acolo. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />
        <!-- AdMob App ID real, din consola AdMob (app "Sodo Quizz"). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7925849908413802~7115439770"/>
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/game_services_project_id"/>
        <meta-data
            android:name="com.chunkytofustudios.firebase_auth_games_services.OAUTH_2_WEB_CLIENT_ID"
            android:value="@string/firebase_auth_oauth_2_web_client_id"/>

        <!-- Pluginul de notificari locale: primeste alarma programata si
             afiseaza notificarea chiar daca aplicatia e inchisa. Al doilea
             receiver le pune la loc dupa o repornire de telefon. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
