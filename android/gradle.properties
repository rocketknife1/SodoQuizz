org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=false
# NU pune aici org.gradle.java.home. Până la 2026-09-09 exista o cale fixă
# spre JDK-ul din Android Studio; la reinstalarea Windows-ului, Android
# Studio n-a mai fost instalat, calea a devenit moartă și ORICE build pica.
# Gradle 8.14 (triada fixă a proiectului) nu merge cu Java 25 — Flutter cere
# Gradle >=9.1.0 pentru asta — dar JDK-ul pentru Gradle se alege per MAȘINĂ,
# nu în repo: `flutter config --jdk-dir=<calea spre un JDK 21>`.
# Verifici cu `flutter doctor -v` (linia "Java binary at").
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
